{
  "name": "raw",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && esbuild main.js --bundle --minify --outfile=dist/main.js && cp index.html dist/index.html"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}
